# Disable lifecycle scripts globally.
# pnpm overrides this via onlyBuiltDependencies (its intended use-case).
# npm global install on Windows would otherwise fail on native packages
# that try to spawn cmd.exe (errno -4058) during preinstall/postinstall.
ignore-scripts=true
