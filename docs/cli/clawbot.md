---
summary: "CLI reference for `Ironcliw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `Ironcliw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `Ironcliw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `Ironcliw clawbot qr` (same behavior as [`Ironcliw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `Ironcliw clawbot qr` -> `Ironcliw qr`

